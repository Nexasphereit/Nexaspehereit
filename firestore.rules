rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // For sandboxed testing and development, allow open read/write access to all collections and documents
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
